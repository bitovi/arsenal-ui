<h2>Inbox {{inboxItemCount}}</h2>

<div class="setMaxHeight"><rn-inbox-grid rows="{inboxRows}" appstate="{appstate}" id="inboxGrid"></rn-inbox-grid></div>
<h2>Outbox {{outboxItemCount}}</h2>
<div class="setMaxHeight"><rn-outbox-grid rows="{outboxRows}" id="outboxGrid"></rn-outbox-grid></div>

import Model from 'can/model/';

// TODO: there's probably some metadata stuff to take care of in here, later.
var PaymentBundleDetail = Model.extend({
  id: "bndlLineId"
}, {});

export default PaymentBundleDetail;
